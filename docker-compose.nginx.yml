version: '3'

services:
    nginx:
        build: 
            context: /home/nginxserver
            dockerfile: Dockerfile
        image: nginx:webserver
        ports: 
            - "80:80"
        container_name: nginx_webserver
        networks:
            - backend

networks:
    backend:
        driver: bridge