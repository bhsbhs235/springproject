version: '3'

services:
    springproject:
        image: bhsbhs235/springproject:0.1
        ports: 
            - "8081:8080"
        container_name: springproject_green
        networks:
            - backend


networks:
    backend:
        driver: bridge